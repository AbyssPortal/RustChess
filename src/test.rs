use chess::chess::*;
use chess_io::*;
#[cfg(test)]
mod test {
    //returns true if output matches input.
    fn generic_test(moves: Vec<&str>, expected_output: &str) -> bool {
        let board = make_default_board();
    }

    #[test]
    fn kingside_castles_test() {}
}
